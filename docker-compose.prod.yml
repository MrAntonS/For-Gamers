services:
  frontend:
    image: gamer-nexus-frontend:latest
    ports:
      - "80:80"
    depends_on:
      - backend

  backend:
    image: gamer-nexus-backend:latest
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
