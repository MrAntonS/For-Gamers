version: '3.8'

services:
  frontend:
    image: gamer-nexus-frontend:latest
    ports:
      - "80:80"

  backend:
    image: gamer-nexus-backend:latest
    ports:
      - "5000:5000"
    environment:
      - FLASK_APP=app.py
      - FLASK_ENV=production
